---
  - hosts: workstation
    tasks:

     - name: bootstrap servers
       shell: cd /root/chef-autoorg; knife bootstrap {{ item }}.example.com -N {{ item }}  -x centos -i /etc/ansible/keys/devops.key --sudo
       with_items:
         - workstation01
         - chefcompliance01
         - chefserver01
         - chefautomate01
         - jenkins01
         - splunk01
         - acceptance01
         - union01
         - rehearsal01
         - delivered01
